{
  "name": "church-fellowship-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd gbi-frontend && npm install && npm run build && cd .. && mkdir -p dist && cp -rv gbi-frontend/build/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.4.0",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "mongodb-memory-server": "^8.14.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}